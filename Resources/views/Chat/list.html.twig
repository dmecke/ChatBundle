<ul id="chat">
{% for message in messages %}
    <li>
        <div class="author"><span>{{ message.author.username }}</span> <span class="date">({{ time_diff(message.insertDate) }})</span></div>
        <div class="message">{{ message.message }}</div>
    </li>
{% endfor %}
</ul>
